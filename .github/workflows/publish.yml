name: Publish to Maven Central (macOS)

on:
  push:
    branches:
      - master

jobs:
  publish:
    runs-on: macos-latest # Spécifie un environnement macOS

    steps:
      # Étape 1 : Vérifier le dépôt
      - name: Checkout code
        uses: actions/checkout@v3

      # Étape 2 : Configurer Java
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '17' # Remplacez par la version de Java utilisée par votre projet
          distribution: 'temurin'

      # Étape 3 : Ajouter les secrets comme variables d'environnement
      - name: Set up environment variables
        env:
          ORG_GRADLE_PROJECT_MAVENCENTRALUSERNAME: ${{ secrets.ORG_GRADLE_PROJECT_MAVENCENTRALUSERNAME }}
          ORG_GRADLE_PROJECT_MAVENCENTRALPASSWORD: ${{ secrets.ORG_GRADLE_PROJECT_MAVENCENTRALPASSWORD }}
          ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEY: ${{ secrets.ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEY }}
          ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEYID: ${{ secrets.ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEYID }}
          ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEYPASSWORD: ${{ secrets.ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEYPASSWORD }}
        run: echo "Environment variables set"

      - name: Verify environment variables
        run: |
          echo "Checking environment variables..."
          [[ -n "${ORG_GRADLE_PROJECT_MAVENCENTRALUSERNAME}" ]] || (echo "ORG_GRADLE_PROJECT_MAVENCENTRALUSERNAME is not set" && exit 1)
          [[ -n "${ORG_GRADLE_PROJECT_MAVENCENTRALPASSWORD}" ]] || (echo "ORG_GRADLE_PROJECT_MAVENCENTRALPASSWORD is not set" && exit 1)
          [[ -n "${ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEY}" ]] || (echo "ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEY is not set" && exit 1)
          [[ -n "${ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEYID}" ]] || (echo "ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEYID is not set" && exit 1)
          [[ -n "${ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEYPASSWORD}" ]] || (echo "ORG_GRADLE_PROJECT_SIGNINGINMEMORYKEYPASSWORD is not set" && exit 1)


      # Étape 4 : Exécuter la commande Gradle
      - name: Publish and Release to Maven Central
        run: ./gradlew publishAndReleaseToMavenCentral --no-configuration-cache
